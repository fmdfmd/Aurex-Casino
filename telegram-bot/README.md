# 🤖 AUREX Support Bot

AI-powered Telegram бот поддержки для казино AUREX с тикет-системой и админ-панелью.

## 🚀 Возможности

- **AI-ассистент:** Отвечает на вопросы игроков через OpenRouter (Claude/GPT)
- **Тикет-система:** Создание тикетов, назначение менеджерам, переписка
- **Менеджеры:** Добавление/удаление операторов, онлайн/офлайн статус
- **Админ-панель:** Управление менеджерами, просмотр статистики
- **FAQ и кнопки:** Готовые ответы на частые вопросы

## 📦 Установка

### 1. Установить зависимости
```bash
cd telegram-bot
npm install
```

### 2. Настроить .env
```bash
cp .env.example .env
```

Заполни файл `.env`:
```env
BOT_TOKEN=123456:ABC...       # Токен от @BotFather
OPENROUTER_API_KEY=sk-...     # Ключ от OpenRouter
DATABASE_URL=postgresql://... # URL базы данных (та же, что у казино)
ADMIN_IDS=123456789           # Твой Telegram ID (через запятую, если несколько)
WEBSITE_URL=https://aurex.casino
CHANNEL_URL=https://t.me/aurex_official
```

### 3. Запустить миграции
```bash
npm run migrate
```

### 4. Запустить бота
```bash
npm start
```

Для разработки с автоперезагрузкой:
```bash
npm run dev
```

## 👑 Команды

### Для всех:
- `/start` — Главное меню

### Для менеджеров:
- `/manager` — Панель менеджера

### Для админов:
- `/admin` — Админ-панель

## 🔐 Как добавить себя админом?

1. Узнай свой Telegram ID (напиши боту @userinfobot)
2. Добавь ID в `.env` в поле `ADMIN_IDS`
3. Перезапусти бота

## 🛡 Как добавить менеджера?

1. Напиши `/admin` в боте
2. Нажми "➕ Добавить менеджера"
3. Перешли сообщение от пользователя (или отправь его Telegram ID)
4. Готово! Пользователь получит уведомление

## 📊 Как работает тикет-система?

1. **Игрок** нажимает "Позвать оператора"
2. **Игрок** описывает проблему
3. **Бот** создаёт тикет и уведомляет всех менеджеров
4. **Менеджер** нажимает "Взять тикет"
5. Начинается переписка через бота
6. **Менеджер** нажимает "Закрыть тикет"
7. **Игрок** оценивает работу оператора

## 🚂 Деплой на Railway

1. Создай новый сервис на Railway
2. Подключи репозиторий
3. Добавь переменные окружения
4. Railway автоматически запустит бота

## 📁 Структура проекта

```
telegram-bot/
├── bot.js              # Основной файл бота
├── config.js           # Конфигурация
├── keyboards.js        # Клавиатуры и кнопки
├── package.json
├── .env.example
├── migrations/
│   ├── 001_support_tables.sql
│   └── run.js
└── services/
    ├── database.js     # Работа с PostgreSQL
    └── ai.js           # OpenRouter AI
```

## 💡 Настройка AI

AI использует OpenRouter. Можно изменить модель в `config.js`:

```javascript
aiModel: 'anthropic/claude-3-haiku', // Быстрый и дешевый
// aiModel: 'anthropic/claude-3-sonnet', // Умнее, дороже
// aiModel: 'openai/gpt-4-turbo', // GPT-4
```

System Prompt (характер бота) настраивается там же в `config.js`.

---

🎰 **AUREX Casino** — Премиальное онлайн-казино
